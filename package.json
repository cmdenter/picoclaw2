{
  "name": "picoclaw",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prebuild": "npm run prebuild --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/frontend"
  ]
}
